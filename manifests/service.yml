apiVersion: v1
kind: Service
metadata:
    name: mukeshupretidockerhelloworldspringboot
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: mukeshupretidockerhelloworldspringboot